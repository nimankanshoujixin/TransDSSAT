# Optional server dependencies for running training and real-model adapters.
# Install only what matches the server runtime you choose.

# Training stack
torch

# Common scientific stack
numpy
pandas

# DSSAT itself is installed separately on the server.
# Recommended backend:
# - official DSSAT runtime from DSSAT/dssat-csm-os
# - a template workspace or preprocessor script that turns TransDSSAT manifests
#   into runnable DSSAT experiment files
