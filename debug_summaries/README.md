# DSSAT Debug Summaries

This directory stores reusable debug summaries for DSSAT integration and replay issues.

Use it for:

- root-cause writeups after a bug is confirmed
- runtime compatibility notes that are likely to recur on new crop models
- links to concrete reports, scripts, and artifact directories

Keep each summary focused on one failure chain so future debugging can reuse the result directly.
